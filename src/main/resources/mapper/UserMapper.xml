<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.UserMapper">

    <select id="findById" resultType="com.example.demo.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.example.demo.entity.User">
        SELECT * FROM user
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, email, age, create_time, update_time)
        VALUES (#{username}, #{email}, #{age}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="age != null">age = #{age},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="findByUsername" resultType="com.example.demo.entity.User">
        SELECT * FROM user WHERE username LIKE CONCAT('%', #{username}, '%')
    </select>

    <select id="findByEmail" resultType="com.example.demo.entity.User">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM user
    </select>

    <insert id="saveAll">
        INSERT INTO user (username, email, age, create_time, update_time) VALUES
        <foreach collection="list" item="user" separator=",">
            (#{user.username}, #{user.email}, #{user.age}, #{user.createTime}, #{user.updateTime})
        </foreach>
    </insert>

    <delete id="deleteAll">
        DELETE FROM user
    </delete>

</mapper>
